#include "main.h"

/**
 * print_last_digit - print the last digit of a num
 * @n: the last digit of a the number
 *
 * Return: the value of the last digit
 */

int print_last_digit(int n)
{
	if (n == the last digit of a num)
		return (n);
	else
		return (-n);
}
